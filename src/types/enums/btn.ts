export enum ButtonVariant {
  DEFAULT = 'default',
  SMOOTH = 'smooth',
  OUTLINED = 'outlined',
  CLEAR = 'clear',
  TEXT = 'text',
}

export enum ButtonSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}

export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}
